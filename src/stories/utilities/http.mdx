import { Meta } from '@storybook/blocks';

<Meta title="Utilities/Http" />

# Http Utilities
## `useAbortControllerSignal()`
Returns an abort controller signal to be provided to an HTTP fetch, and automatically aborts the request upon unmount. The same abort signal can be provided to multiple HTTP requests.
## `postRequest(PostFormDataRequestParams)`
## `postFormDataRequest(BaseRequestParams)`
## `getRequest(BaseRequestParams)`
## `deleteRequest(GetRequestParams)`
## `putRequest(PostRequestParams)`
## `putFormDataRequest(PostFormDataRequestParams)`


